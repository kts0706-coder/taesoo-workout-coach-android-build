name: Build Android APKon:  push:    branches: [main]  workflow_dispatch:permissions:  contents: readjobs:  build:    runs-on: ubuntu-latest    steps:      - name: Check out source        uses: actions/checkout@v4      - name: Extract verified v2.9.1 source        run: unzip -q kim-taesoo-workout-coach-android-v22-source.zip      - name: Set up Java 17        uses: actions/setup-java@v4        with:          distribution: temurin          java-version: "17"      - name: Set up Gradle        uses: gradle/actions/setup-gradle@v4        with:          gradle-version: "8.11.1"      - name: Build debug APK        working-directory: android-app        run: gradle --no-daemon assembleDebug      - name: Upload APK
